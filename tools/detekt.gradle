detekt {
    config = files("$rootDir/tools/default-detekt-config.yml")
    parallel = true
    autoCorrect = true
    //Optional baseline, uncomment & run gradle command detektBaseline to exclude existing issues
//    baseline = file("$rootDir/tools/detekt-baseline.xml")
}
